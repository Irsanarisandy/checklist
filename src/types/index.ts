export interface Task {
	id: string;
	title: string;
	status: string;
	category: string;
	priority: number;
	cta_text: string;
	do_when: string;
	estimated_time: string;
	short_description: string;
	long_description: string;
}
